# Firecrawl Lite - 环境配置 / Environment Configuration

# 服务器 / Server
PORT=3000
NODE_ENV=development
DOMAIN=your-domain.com  # Production domain for Caddy auto-HTTPS

# 部署 / Deployment (for CI/CD)
# NOTE: All deployment variables below are REQUIRED and must be set in CI/CD environment
REMOTE_HOST=your-vps-ip              # REQUIRED: Target server IP or hostname
REMOTE_USERNAME=root                 # REQUIRED: SSH user (usually root for VPS)
REMOTE_PORT=22                       # Optional: SSH port (default 22)
# DEPLOY_SECRETS_IMPORTS=<required>  # REQUIRED: Path to secrets YAML in CNB project-secrets

# 安全 / Security
API_KEY=your_secret_key_here

# 性能 / Performance
MAX_BROWSERS=5  # 默认5（与代码一致）
MAX_CRAWL_DEPTH=3
MAX_CRAWL_PAGES=50

# 日志 / Logging
LOG_LEVEL=info

# CORS（仅跨域部署时需要 / Only for cross-domain deployment）
# ALLOWED_ORIGINS=https://example.com,https://app.example.com
