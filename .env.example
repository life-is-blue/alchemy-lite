# Firecrawl Lite - 环境配置 / Environment Configuration

# 服务器 / Server
PORT=3000
NODE_ENV=development
DOMAIN=example.com  # Production domain for Caddy auto-HTTPS

# 安全 / Security
API_KEY=your_secret_key_here

# 性能 / Performance
MAX_BROWSERS=5  # 默认5（与代码一致）
MAX_CRAWL_DEPTH=3
MAX_CRAWL_PAGES=50

# 日志 / Logging
LOG_LEVEL=info

# CORS（仅跨域部署时需要 / Only for cross-domain deployment）
# ALLOWED_ORIGINS=https://example.com,https://app.example.com
