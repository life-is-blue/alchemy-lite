# Firecrawl Lite - 环境配置 / Environment Configuration
# 
# Philosophy: Variables here are for LOCAL DEVELOPMENT only.
# CI/CD deployment secrets are managed via CNB platform (not .env).

# ============================================================================
# Application Settings / 应用设置
# ============================================================================

# Server Port
# 本地开发用。容器内部始终使用 3000。
PORT=3000

# Environment
# development: 日志详细，允许 CORS 测试
# production: 日志简洁，安全限制
NODE_ENV=development

# Domain for HTTPS (Production only)
# 用于 Caddy 自动申请 SSL 证书。本地开发无需修改。
DOMAIN=your-domain.com

# ============================================================================
# API Security / API 安全
# ============================================================================

# Optional API Key for authentication
# 如果设置，所有请求必须在 header 中提供: Authorization: Bearer <API_KEY>
# 留空则禁用认证（开发模式）
API_KEY=

# ============================================================================
# Performance Tuning / 性能调优
# ============================================================================

# Browser pool size (Puppeteer instances)
# 推荐值: 3-10（取决于服务器内存）
# 每个浏览器实例占用 ~100-150MB
MAX_BROWSERS=5

# ============================================================================
# Logging / 日志
# ============================================================================

# Log Level
# debug: 最详细（不推荐生产环境）
# info: 标准（默认）
# warn: 仅警告和错误
# error: 仅错误
LOG_LEVEL=info

# ============================================================================
# CORS Configuration / CORS（可选）
# ============================================================================

# Allowed origins for CORS
# 仅当前端部署在不同域名时需要
# 示例: https://app.example.com,https://dashboard.example.com
# ALLOWED_ORIGINS=

# ============================================================================
# API Request Limits / API 请求限制
# ============================================================================

# 注意: 以下限制由 API 请求参数控制，不是环境变量：
# - maxDepth: 爬虫最大深度（默认 3）
# - maxPages: 爬虫最大页数（默认 50）
# 在 POST /api/crawl 时指定这些参数可以覆盖默认值。
