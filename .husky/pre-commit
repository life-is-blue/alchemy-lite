# Run tests before commit
npm test

# Check for .approved-commit file (code review requirement)
if [ ! -f ".approved-commit" ]; then
  echo "❌ COMMIT BLOCKED: No .approved-commit file found"
  echo "Code must be reviewed before commit. Run code review first."
  exit 1
fi

echo "✅ Pre-commit checks passed"